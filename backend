server {
        listen 443 ssl;
        server_name backend2.dries007.net;

        location / { try_files $uri @backend; }
        location @backend {
                include uwsgi_params;
                uwsgi_pass unix:/tmp/backend.sock;
        }
}
